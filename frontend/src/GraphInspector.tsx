import type { Agent, GraphNode } from "./api";

function Schema({ value }: { value: Record<string, unknown> }) { return <pre>{JSON.stringify(value, null, 2)}</pre>; }
function AgentDetails({ agent }: { agent: Agent }) { return <><p>{agent.description}</p><dl><div><dt>Prompt</dt><dd>{agent.prompt ?? agent.prompt_excerpt ?? "Not exposed"}</dd></div><div><dt>Model</dt><dd>{agent.provider} / {agent.model}</dd></div><div><dt>Generation</dt><dd>Temperature {agent.temperature}; {agent.max_output_tokens} tokens</dd></div><div><dt>Tools</dt><dd>{agent.tools.join(", ") || "None"}</dd></div><div><dt>Policy</dt><dd>{agent.timeout_seconds}s timeout; {agent.max_attempts} attempts</dd></div><div><dt>Schemas</dt><dd>{agent.input_schema} → {agent.output_schema}</dd></div><div><dt>Authority</dt><dd>{agent.authority_badges.join(", ") || "None"}</dd></div><div><dt>Configuration hash</dt><dd><code>{agent.config_hash}</code></dd></div></dl><h3>Input schema</h3><Schema value={agent.input_schema_json} /><h3>Output schema</h3><Schema value={agent.output_schema_json} /></>; }
export function GraphInspector({ selection }: { selection?: { stage: GraphNode } }) { if (!selection) return <><h2>Inspector</h2><p>Select or focus a graph stage to inspect its safe configuration.</p></>; const { stage } = selection; return <><p className="eyebrow">Stage inspector</p><h2>{stage.node_id}</h2>{stage.agent ? <AgentDetails agent={stage.agent} /> : <p>{stage.description}</p>}</>; }
